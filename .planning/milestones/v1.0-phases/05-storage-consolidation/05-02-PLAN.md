---
phase: 05-storage-consolidation
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/components/discovery/ExerciseMode.tsx
  - src/components/discovery/ImageMode.tsx
  - src/components/history/HistoryPage.tsx
  - src/components/library/LibraryPage.tsx
  - src/components/live-roleplay/ConversationAnalysis.tsx
  - src/components/live-roleplay/ScenarioSetup.tsx
  - src/components/paths/PathsPage.tsx
  - src/components/practice/PracticePage.tsx
  - src/components/review/ReviewPage.tsx
  - src/components/settings/SettingsPage.tsx
  - src/services/errorAnalysis.ts
  - src/services/gamification.ts
  - src/services/supabase/index.ts
autonomous: true
requirements:
  - STOR-01
  - STOR-02

must_haves:
  truths:
    - "All 13 supabase/storage import sites now import from the single facade module"
    - "No file imports from both storage.ts and supabase/storage.ts simultaneously"
    - "ConversationAnalysis.tsx has a single import from ../../services/storage"
    - "Supabase barrel re-exports carry @deprecated JSDoc comments"
  artifacts:
    - path: "src/components/live-roleplay/ConversationAnalysis.tsx"
      provides: "No more dual storage import"
      contains: "from '../../services/storage'"
    - path: "src/services/supabase/index.ts"
      provides: "Deprecated storage re-exports"
      contains: "@deprecated"
  key_links:
    - from: "src/components/library/LibraryPage.tsx"
      to: "src/services/storage.ts"
      via: "import redirection"
      pattern: "from '../../services/storage'"
    - from: "src/components/settings/SettingsPage.tsx"
      to: "src/services/storage.ts"
      via: "import redirection"
      pattern: "from '../../services/storage'"
---

<objective>
Migrate all 13 supabase/storage import sites to import from the facade (`storage.ts`) instead. Fix the dual import in ConversationAnalysis.tsx. Add @deprecated JSDoc to supabase/index.ts barrel re-exports.

Purpose: Complete STOR-01 — every storage import in the codebase resolves to the single facade module.
Output: 13 files with redirected imports + 1 barrel file with deprecation comments
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-storage-consolidation/05-CONTEXT.md
@.planning/phases/05-storage-consolidation/05-RESEARCH.md
@.planning/phases/05-storage-consolidation/05-01-SUMMARY.md

<interfaces>
<!-- Import migration map — exact file, old import path, new import path, functions imported -->

Complete migration map (13 files):
```
File                                           | Old Import Path                         | New Import Path                    | Functions
src/components/discovery/ExerciseMode.tsx      | ../../services/supabase/storage         | ../../services/storage              | addCard
src/components/discovery/ImageMode.tsx         | ../../services/supabase/storage         | ../../services/storage              | addCard
src/components/history/HistoryPage.tsx         | ../../services/supabase/storage         | ../../services/storage              | clearLiveSessions, getLiveSessions
src/components/library/LibraryPage.tsx         | ../../services/supabase/storage         | ../../services/storage              | getCards, deleteCard, updateCard, addCard
src/components/live-roleplay/ConversationAnalysis.tsx | ../../services/supabase/storage  | ../../services/storage (MERGE)      | saveLiveSession (merge with existing getModelConfig import)
src/components/live-roleplay/ScenarioSetup.tsx | ../../services/supabase/storage         | ../../services/storage              | getUserContext
src/components/paths/PathsPage.tsx             | ../../services/supabase/storage         | ../../services/storage              | getPathProgress, markStepComplete
src/components/practice/PracticePage.tsx       | ../../services/supabase/storage         | ../../services/storage              | getConversationTone, getUserContext
src/components/review/ReviewPage.tsx           | ../../services/supabase/storage         | ../../services/storage              | getCardsDueForReview, updateCard
src/components/settings/SettingsPage.tsx       | ../../services/supabase/storage         | ../../services/storage              | getModelConfig, saveModelConfig, getConversationTone, saveConversationTone, getUserContext, saveUserContext, saveApiKeys
src/services/errorAnalysis.ts                  | ./supabase/storage                      | ./storage                           | getCards
src/services/gamification.ts                   | ./supabase/storage                      | ./storage                           | getCards, getGamification, saveGamification, saveSessionReport
```

Special case — ConversationAnalysis.tsx (DUAL IMPORT):
```
Current line 4: import { getModelConfig } from '../../services/storage';
Current line 5: import { saveLiveSession } from '../../services/supabase/storage'
After:           import { getModelConfig, saveLiveSession } from '../../services/storage';
```
Remove line 5, merge saveLiveSession into line 4's import.

IMPORTANT: runtimeState.ts imports from ./supabase/storage — DO NOT change this. runtimeState is the hydration layer that must import directly from the implementation.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate all supabase/storage import sites to facade</name>
  <files>
    src/components/discovery/ExerciseMode.tsx,
    src/components/discovery/ImageMode.tsx,
    src/components/history/HistoryPage.tsx,
    src/components/library/LibraryPage.tsx,
    src/components/live-roleplay/ConversationAnalysis.tsx,
    src/components/live-roleplay/ScenarioSetup.tsx,
    src/components/paths/PathsPage.tsx,
    src/components/practice/PracticePage.tsx,
    src/components/review/ReviewPage.tsx,
    src/components/settings/SettingsPage.tsx,
    src/services/errorAnalysis.ts,
    src/services/gamification.ts,
    src/services/supabase/index.ts
  </files>
  <read_first>
    src/components/discovery/ExerciseMode.tsx
    src/components/discovery/ImageMode.tsx
    src/components/history/HistoryPage.tsx
    src/components/library/LibraryPage.tsx
    src/components/live-roleplay/ConversationAnalysis.tsx
    src/components/live-roleplay/ScenarioSetup.tsx
    src/components/paths/PathsPage.tsx
    src/components/practice/PracticePage.tsx
    src/components/review/ReviewPage.tsx
    src/components/settings/SettingsPage.tsx
    src/services/errorAnalysis.ts
    src/services/gamification.ts
    src/services/supabase/index.ts
  </read_first>
  <action>
    For each of the 12 consumer files (excluding supabase/index.ts), change the import path:

    Component files: change `'../../services/supabase/storage'` to `'../../services/storage'`
    Service files: change `'./supabase/storage'` to `'./storage'`

    Special handling for ConversationAnalysis.tsx:
    - Line 4: `import { getModelConfig } from '../../services/storage';` — ADD `saveLiveSession` to this import
    - Line 5: `import { saveLiveSession } from '../../services/supabase/storage'` — DELETE this entire line
    - Result: `import { getModelConfig, saveLiveSession } from '../../services/storage';`

    DO NOT change these files (they must keep importing from supabase/storage directly):
    - src/services/runtimeState.ts — this is the hydration layer that feeds the cache
    - src/services/supabase/index.ts barrel — keep re-exporting from ./storage (supabase/storage.ts)

    For supabase/index.ts — add @deprecated JSDoc comment block ABOVE the "// Storage" section comment:
    ```typescript
    /**
     * @deprecated Import from 'services/storage' instead of 'services/supabase'.
     * The storage.ts facade provides the same functions with auth-aware routing.
     */
    ```
    The re-exports themselves stay intact per D-04 (barrel continues during transition).
  </action>
  <verify>
    <automated>grep -rn "from.*supabase/storage" src/components/ src/services/errorAnalysis.ts src/services/gamification.ts 2>/dev/null | grep -v "node_modules" | wc -l</automated>
  </verify>
  <done>
    - Zero component or service files import from supabase/storage (only runtimeState.ts and supabase/index.ts still import from it)
    - ConversationAnalysis.tsx has a single import from ../../services/storage with both getModelConfig and saveLiveSession
    - supabase/index.ts has @deprecated JSDoc on storage re-exports
    - TypeScript compiles with zero errors
    - All existing tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Final verification — zero dual imports and compile check</name>
  <files>
  </files>
  <read_first>
    src/components/live-roleplay/ConversationAnalysis.tsx
    src/services/storage.ts
  </read_first>
  <action>
    Run the following verification checks and fix any issues found:

    1. Verify no file imports from BOTH storage.ts AND supabase/storage.ts:
       `grep -rn "from.*services/storage" src/ --include="*.ts" --include="*.tsx" | grep -v node_modules`
       For each file found, ensure it only has one storage import line.

    2. Verify supabase/storage is only imported by:
       - src/services/storage.ts (the facade)
       - src/services/runtimeState.ts (the hydration layer)
       - src/services/supabase/index.ts (the barrel)
       `grep -rn "from.*supabase/storage" src/ --include="*.ts" --include="*.tsx"`
       If any OTHER file appears, redirect it to the facade.

    3. Run TypeScript compilation: `npx tsc --noEmit`

    4. Run full test suite: `npx vitest run`

    Fix any issues found before marking done.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <done>
    - Zero files have dual storage imports
    - supabase/storage imports exist only in: storage.ts, runtimeState.ts, supabase/index.ts
    - TypeScript compiles with zero errors
    - All tests pass
  </done>
</task>

</tasks>

<verification>
1. `grep -rn "from.*supabase/storage" src/ --include="*.ts" --include="*.tsx"` — only shows storage.ts, runtimeState.ts, supabase/index.ts
2. `grep -rn "from.*services/storage" src/components/ src/services/errorAnalysis.ts src/services/gamification.ts` — all 12 files import from the facade
3. No file has both `from.*services/storage` and `from.*supabase/storage` in the same file
4. `npx tsc --noEmit` — zero errors
5. `npx vitest run` — all tests pass
</verification>

<success_criteria>
- All 13 former supabase/storage import sites now import from the facade (storage.ts)
- ConversationAnalysis.tsx has a single merged import (no dual import)
- supabase/index.ts barrel carries @deprecated JSDoc on storage section
- TypeScript compiles cleanly
- All tests pass
- Only 3 files still import directly from supabase/storage.ts: facade (storage.ts), hydration (runtimeState.ts), barrel (supabase/index.ts)
</success_criteria>

<output>
After completion, create `.planning/phases/05-storage-consolidation/05-02-SUMMARY.md`
</output>
