---
phase: 07-dead-code-facade-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/errors/ChunkErrorFallback.tsx
  - src/components/errors/__tests__/ChunkErrorFallback.test.tsx
  - src/services/openaiRealtimeLive.ts
  - src/services/openaiRealtimeLive.test.ts
  - src/services/supabase/aiProxy.ts
  - src/services/supabase/index.ts
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "ChunkErrorFallback.tsx no longer exists; ErrorFallback.tsx still handles chunk errors"
    - "OpenAIRealtimeLiveSession no longer exists; GeminiLiveSession still works for live roleplay"
    - "aiProxy.ts has no orphaned exports (getGeminiKeyForLive, getVertexLiveToken, withFallback removed)"
    - "supabase/index.ts barrel file has no stale re-exports"
    - "TypeScript build succeeds with zero errors after all deletions"
  artifacts:
    - path: "src/components/errors/ChunkErrorFallback.tsx"
      provides: "DELETED - orphaned component"
      must_not_exist: true
    - path: "src/components/errors/__tests__/ChunkErrorFallback.test.tsx"
      provides: "DELETED - orphaned test"
      must_not_exist: true
    - path: "src/services/openaiRealtimeLive.ts"
      provides: "DELETED - unused live session class"
      must_not_exist: true
    - path: "src/services/openaiRealtimeLive.test.ts"
      provides: "DELETED - test for unused class"
      must_not_exist: true
    - path: "src/services/supabase/aiProxy.ts"
      provides: "AI proxy with orphaned exports removed"
      must_not_contain: ["getGeminiKeyForLive", "getVertexLiveToken", "withFallback"]
    - path: "src/services/supabase/index.ts"
      provides: "Barrel file with orphaned re-exports removed"
      must_not_contain: ["getGeminiKeyForLive", "withFallback"]
  key_links:
    - from: "src/services/supabase/index.ts"
      to: "src/services/supabase/aiProxy.ts"
      via: "re-export block (lines 77-90)"
      pattern: "export.*from.*aiProxy"
---

<objective>
Remove all orphaned dead code identified in the v1.0 milestone audit.

Purpose: Reduce bundle size, eliminate confusing unused code, and complete the cleanup that was deferred during v1.0 shipping. Three independent dead code targets: an orphaned error component, an unused live session class, and three zero-consumer exports from aiProxy.

Output: 4 files deleted, 2 files cleaned up, TypeScript build green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-dead-code-facade-cleanup/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete orphaned ChunkErrorFallback component and test</name>
  <files>
    src/components/errors/ChunkErrorFallback.tsx
    src/components/errors/__tests__/ChunkErrorFallback.test.tsx
  </files>
  <read_first>
    src/components/errors/ChunkErrorFallback.tsx
    src/components/errors/__tests__/ChunkErrorFallback.test.tsx
    src/components/errors/ErrorFallback.tsx
  </read_first>
  <action>
    Delete these two files:
    - `src/components/errors/ChunkErrorFallback.tsx` (30 lines, orphaned component)
    - `src/components/errors/__tests__/ChunkErrorFallback.test.tsx` (orphaned test)

    These files are safe to delete because:
    - ZERO production imports of ChunkErrorFallback (confirmed via grep)
    - Only its own test file imports it
    - ErrorFallback.tsx already handles chunk errors inline via `isChunkError()` helper and `navigate(0)` retry

    Do NOT modify ErrorFallback.tsx -- it is listed in read_first for verification only.
  </action>
  <verify>
    <automated>test ! -f src/components/errors/ChunkErrorFallback.tsx && test ! -f src/components/errors/__tests__/ChunkErrorFallback.test.tsx && echo "DELETION OK" || echo "FILES STILL EXIST"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/errors/ChunkErrorFallback.tsx` does NOT exist on disk
    - `src/components/errors/__tests__/ChunkErrorFallback.test.tsx` does NOT exist on disk
    - `grep -r "ChunkErrorFallback" src/` returns zero results
  </acceptance_criteria>
  <done>Both ChunkErrorFallback files deleted. No production code references them.</done>
</task>

<task type="auto">
  <name>Task 2: Delete unused OpenAIRealtimeLiveSession class and test</name>
  <files>
    src/services/openaiRealtimeLive.ts
    src/services/openaiRealtimeLive.test.ts
  </files>
  <read_first>
    src/services/openaiRealtimeLive.ts
    src/services/openaiRealtimeLive.test.ts
    src/services/liveSession.ts
  </read_first>
  <action>
    Delete these two files:
    - `src/services/openaiRealtimeLive.ts` (unused OpenAI Realtime live session implementation)
    - `src/services/openaiRealtimeLive.test.ts` (test for unused class)

    These files are safe to delete because:
    - ZERO production imports of OpenAIRealtimeLiveSession (confirmed via grep)
    - Production live roleplay uses ONLY GeminiLiveSession (hardcoded in LiveSession.tsx lines 44, 60)
    - Only its own test file imports it

    Do NOT modify `src/services/liveSession.ts` -- the `ILiveSession` interface it exports is still used by GeminiLiveSession and must be kept.
  </action>
  <verify>
    <automated>test ! -f src/services/openaiRealtimeLive.ts && test ! -f src/services/openaiRealtimeLive.test.ts && echo "DELETION OK" || echo "FILES STILL EXIST"</automated>
  </verify>
  <acceptance_criteria>
    - `src/services/openaiRealtimeLive.ts` does NOT exist on disk
    - `src/services/openaiRealtimeLive.test.ts` does NOT exist on disk
    - `grep -r "OpenAIRealtimeLiveSession\|openaiRealtimeLive" src/` returns zero results
    - `src/services/liveSession.ts` still exports `ILiveSession` interface
  </acceptance_criteria>
  <done>Both OpenAIRealtimeLiveSession files deleted. ILiveSession interface preserved. No production code references the removed class.</done>
</task>

<task type="auto">
  <name>Task 3: Remove orphaned exports from aiProxy.ts and barrel re-exports from supabase/index.ts</name>
  <files>
    src/services/supabase/aiProxy.ts
    src/services/supabase/index.ts
  </files>
  <read_first>
    src/services/supabase/aiProxy.ts
    src/services/supabase/index.ts
  </read_first>
  <action>
    In `src/services/supabase/aiProxy.ts`, delete these three orphaned functions (lines ~223-280):
    1. `export async function getGeminiKeyForLive()` -- zero consumers, was for Gemini Live WebSocket key retrieval
    2. `export async function getVertexLiveToken()` -- zero consumers, was for Vertex AI live token
    3. `export async function withFallback<T>()` -- zero consumers, was a generic fallback wrapper

    In `src/services/supabase/index.ts`, remove these from the AI Proxy re-export block (lines 77-90):
    1. `getGeminiKeyForLive` (line 83)
    2. `withFallback` (line 84)

    The remaining AI Proxy exports (`chatCompletion`, `chatCompletionWithImage`, `textToSpeech`, `speechToText`, `generateImage`, and the type exports) MUST be kept -- they have active consumers.

    After edits, the AI Proxy export block in index.ts should be:
    ```typescript
    // AI Proxy
    export {
      chatCompletion,
      chatCompletionWithImage,
      textToSpeech,
      speechToText,
      generateImage,
      type ChatCompletionOptions,
      type ChatCompletionWithImageOptions,
      type TextToSpeechOptions,
      type SpeechToTextOptions,
      type ImageGenerationOptions,
    } from './aiProxy'
    ```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "getGeminiKeyForLive\|getVertexLiveToken\|withFallback" src/services/supabase/aiProxy.ts` returns 0
    - `grep -c "getGeminiKeyForLive\|withFallback" src/services/supabase/index.ts` returns 0
    - `npx tsc --noEmit` exits with code 0 (no TypeScript errors)
    - `grep "chatCompletion" src/services/supabase/index.ts` still shows the export (kept exports intact)
  </acceptance_criteria>
  <done>Three orphaned functions removed from aiProxy.ts. Two stale re-exports removed from barrel file. TypeScript build green.</done>
</task>

</tasks>

<verification>
1. All 4 target files deleted (ChunkErrorFallback.tsx, ChunkErrorFallback.test.tsx, openaiRealtimeLive.ts, openaiRealtimeLive.test.ts)
2. `npx tsc --noEmit` passes with zero errors
3. `npx vitest run` passes (existing tests still green)
4. `grep -r "ChunkErrorFallback\|OpenAIRealtimeLiveSession\|getGeminiKeyForLive\|getVertexLiveToken\|withFallback" src/ --include="*.ts" --include="*.tsx"` returns zero results
</verification>

<success_criteria>
- 4 files deleted, 2 files cleaned up
- TypeScript compilation succeeds
- Full test suite passes
- Zero references to removed code anywhere in src/
</success_criteria>

<output>
After completion, create `.planning/phases/07-dead-code-facade-cleanup/07-01-SUMMARY.md`
</output>
