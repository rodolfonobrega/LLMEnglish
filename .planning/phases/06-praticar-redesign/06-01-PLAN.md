---
phase: 06-praticar-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/shared/PracticeModeCard.tsx
  - src/components/shared/PracticeModeCard.test.tsx
  - src/components/practice/PracticeHubPage.tsx
  - src/components/practice/PracticeHubPage.test.tsx
autonomous: false
requirements:
  - VIS-01
  - VIS-02
  - VIS-03

must_haves:
  truths:
    - "Praticar page shows 7 mode cards in 2 sections (Pratica Solo with 5, Ao Vivo with 2)"
    - "Each card displays an image banner on top with label, description, and example text below"
    - "Cards have h-40 image banners, visibly taller than PathCard h-32"
    - "All cards are keyboard navigable: focusable via Tab, activatable via Enter/Space, have aria-label"
    - "Image errors show gradient fallback with mode's Lucide icon instead of broken image"
    - "No ModeTooltip wrapper remains in PracticeHubPage"
  artifacts:
    - path: "src/components/shared/PracticeModeCard.tsx"
      provides: "Image-banner card component for practice modes"
      exports: ["PracticeModeCard"]
    - path: "src/components/practice/PracticeHubPage.tsx"
      provides: "Redesigned practice hub with 2-section vertical layout"
      exports: ["PracticeHubPage"]
    - path: "src/components/shared/PracticeModeCard.test.tsx"
      provides: "Unit tests for PracticeModeCard (VIS-01, VIS-02, VIS-03)"
    - path: "src/components/practice/PracticeHubPage.test.tsx"
      provides: "Unit tests for PracticeHubPage (VIS-01 section grouping)"
  key_links:
    - from: "src/components/practice/PracticeHubPage.tsx"
      to: "src/components/shared/PracticeModeCard.tsx"
      via: "import { PracticeModeCard }"
      pattern: "import.*PracticeModeCard"
    - from: "src/components/practice/PracticeHubPage.tsx"
      to: "src/config/modes.ts"
      via: "import exerciseModes, conversationModes, trailsMode"
      pattern: "import.*modes"
    - from: "src/components/practice/PracticeHubPage.tsx"
      to: "src/components/shared/ModeTooltip.tsx"
      via: "MUST NOT import"
      pattern: "(?!import.*ModeTooltip)^$"
---

<objective>
Redesign the Praticar page from horizontal ModeCard list to vertically-oriented, image-banner cards inspired by PathCard.

Purpose: Deliver a visually polished practice hub that feels magazine-like and is fully keyboard accessible, completing all visual requirements (VIS-01, VIS-02, VIS-03).

Output: New PracticeModeCard component, rewritten PracticeHubPage with 2-section layout, passing test suites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-praticar-redesign/06-CONTEXT.md
@.planning/phases/06-praticar-redesign/06-RESEARCH.md
@.planning/phases/06-praticar-redesign/06-UI-SPEC.md
@.planning/phases/06-praticar-redesign/06-VALIDATION.md
@src/components/ui/custom/PathCard.tsx
@src/components/shared/ModeCard.tsx
@src/components/practice/PracticeHubPage.tsx
@src/config/modes.ts
@src/config/modes.test.ts
@src/test/setup.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/config/modes.ts:
```typescript
export interface PracticeMode {
  id: string;
  label: string;
  description: string;
  example: string;
  colorVar: string;
  icon: LucideIcon;
  to: string;
  image?: string;
  highlighted?: boolean;
}

export const exerciseModes: readonly PracticeMode[]   // phrases, texts, situations, scripts (4 modes)
export const conversationModes: readonly PracticeMode[] // simulation, visual (2 modes)
export const trailsMode: PracticeMode                   // trails (1 mode)
export const allModes: readonly PracticeMode[]          // all 7 modes
```

From src/components/shared/ModeCard.tsx:
```typescript
interface ModeCardProps {
  mode: PracticeMode;
  onClick?: () => void;
  className?: string;
}
// Uses: <button> element, mode.icon, imgError state, hsl(var(--mode-${mode.colorVar})) inline styles
```

From src/components/ui/custom/PathCard.tsx:
```typescript
// Image banner pattern: h-32 div with img + imgError fallback
// Hover: group-hover:scale-105 transition-transform duration-300
// Outer: overflow-hidden rounded-xl border border-border bg-card card-hover card-hover-border
```

From src/utils/cn.ts:
```typescript
export function cn(...inputs: ClassValue[]): string;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create PracticeModeCard component with tests</name>
  <files>src/components/shared/PracticeModeCard.tsx, src/components/shared/PracticeModeCard.test.tsx</files>
  <read_first>
    src/components/shared/ModeCard.tsx
    src/components/ui/custom/PathCard.tsx
    src/config/modes.ts
    src/test/setup.ts
    .planning/phases/06-praticar-redesign/06-UI-SPEC.md
    .planning/phases/06-praticar-redesign/06-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - src/components/shared/PracticeModeCard.tsx exists and contains "export function PracticeModeCard"
    - src/components/shared/PracticeModeCard.test.tsx exists
    - PracticeModeCard.tsx contains '<button' (not just <div>)
    - PracticeModeCard.tsx contains 'aria-label'
    - PracticeModeCard.tsx contains 'h-40'
    - PracticeModeCard.tsx contains 'Ex: {mode.example}' or 'Ex: {' (example prefix)
    - PracticeModeCard.tsx contains 'group-hover:scale-105'
    - PracticeModeCard.tsx does NOT contain rounded corners on image container (no 'rounded-t-xl' inside image div)
    - npx vitest run src/components/shared/PracticeModeCard.test.tsx exits 0
  </acceptance_criteria>
  <behavior>
    - Test: PracticeModeCard renders a <button> element (not a <div>) -- VIS-03
    - Test: PracticeModeCard has aria-label attribute containing mode.label and mode.description -- VIS-03
    - Test: PracticeModeCard renders an <img> element with mode.image src when mode.image is provided -- VIS-01
    - Test: PracticeModeCard renders gradient fallback with Lucide icon when img onError fires -- VIS-01
    - Test: PracticeModeCard renders mode.label text -- VIS-01
    - Test: PracticeModeCard renders mode.description text -- VIS-01
    - Test: PracticeModeCard renders "Ex: " prefix followed by mode.example text -- VIS-01, per D-06
    - Test: PracticeModeCard image container has "h-40" class (per D-02, VIS-02 -- 25% taller than PathCard h-32)
    - Test: PracticeModeCard fires onClick when button is clicked -- VIS-03
    - Test: PracticeModeCard has focus-visible ring classes (focus-visible:ring-2) -- VIS-03
  </behavior>
  <action>
    Create `src/components/shared/PracticeModeCard.test.tsx` FIRST with tests for all behaviors above.
    Use @testing-library/react render() and @testing-library/user-event for interaction tests.
    Import mode fixtures from `src/config/modes.ts` (use exerciseModes[0] as a representative mode).
    For image error test: mock img onError by querying the img element and triggering error event.

    Then create `src/components/shared/PracticeModeCard.tsx` implementing the component:

    1. Props interface: `{ mode: PracticeMode; onClick?: () => void; className?: string }` (per UI-SPEC)
    2. Named export: `export function PracticeModeCard({ mode, onClick, className }: PracticeModeCardProps)`
    3. `useState(false)` for imgError tracking
    4. Outer element: `<button>` (per D-03, VIS-03) with these classes:
       `w-full text-left rounded-xl border border-border bg-card overflow-hidden transition-all duration-200 card-hover card-hover-border focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 cursor-pointer group relative`
    5. `aria-label={mode.label + ': ' + mode.description}` on the button (per UI-SPEC accessibility spec)
    6. Image banner: `<div className="h-40 w-full overflow-hidden bg-muted">` (per D-01, D-02, D-09)
       - When mode.image exists and no imgError: `<img>` with classes `w-full h-full object-cover group-hover:scale-105 transition-transform duration-300`, loading="lazy", onError handler
       - On imgError or no image: gradient fallback div with `background: linear-gradient(135deg, hsl(var(--mode-${mode.colorVar}-soft)), hsl(var(--mode-${mode.colorVar})))` and mode.icon centered as white icon (per D-11, NOT emoji like PathCard)
    7. Content area: `<div className="p-4">` containing:
       - Title: `<span className="font-semibold text-base" style={{ color: 'hsl(var(--mode-${mode.colorVar}))' }}>{mode.label}</span>` (per UI-SPEC typography)
       - Description: `<p className="text-sm text-muted-foreground mt-1">{mode.description}</p>` (per D-04)
       - Example: `<p className="text-xs italic mt-2" style={{ color: 'hsl(var(--mode-${mode.colorVar}))', opacity: 0.8 }}>Ex: {mode.example}</p>` (per D-06)
    8. Use `cn()` for className composition on the outer button (import from `../../utils/cn`)
    9. Import `import type { PracticeMode } from '../../config/modes'` (verbatimModuleSyntax requires `import type`)

    IMPORTANT: The `group` class on the outer button is required for `group-hover:scale-105` to work on the image (Pitfall 3 from RESEARCH.md).
    IMPORTANT: Do NOT add rounded corners on the image div -- overflow-hidden on the parent button handles clipping (Pitfall 2 from RESEARCH.md).
    IMPORTANT: Do NOT hardcode color values. Always use `hsl(var(--mode-${mode.colorVar}))` via inline style.
  </action>
  <verify>
    <automated>npx vitest run src/components/shared/PracticeModeCard.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - PracticeModeCard.tsx exists and exports named function PracticeModeCard
    - All 10 test cases pass
    - Component uses <button> element, has aria-label, has h-40 image banner
    - Gradient fallback renders on image error with Lucide icon (not emoji)
    - Example text rendered with "Ex: " prefix
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite PracticeHubPage with 2-section layout and tests</name>
  <files>src/components/practice/PracticeHubPage.tsx, src/components/practice/PracticeHubPage.test.tsx</files>
  <read_first>
    src/components/practice/PracticeHubPage.tsx
    src/components/shared/PracticeModeCard.tsx
    src/config/modes.ts
    src/config/modes.test.ts
    src/test/setup.ts
    .planning/phases/06-praticar-redesign/06-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - src/components/practice/PracticeHubPage.tsx contains "Pratica Solo" (section header text)
    - src/components/practice/PracticeHubPage.tsx contains "Ao Vivo" (section header text)
    - src/components/practice/PracticeHubPage.tsx contains "import { PracticeModeCard }"
    - src/components/practice/PracticeHubPage.tsx does NOT contain "ModeTooltip"
    - src/components/practice/PracticeHubPage.tsx does NOT contain "ModeCard" (old import)
    - src/components/practice/PracticeHubPage.tsx contains "soloModes" and "liveModes" (inline grouping)
    - src/components/practice/PracticeHubPage.tsx contains "visual" mode in soloModes (via conversationModes.find)
    - npx vitest run src/components/practice/PracticeHubPage.test.tsx exits 0
    - npx vitest run src/components/shared/PracticeModeCard.test.tsx src/components/practice/PracticeHubPage.test.tsx exits 0 (no regression)
  </acceptance_criteria>
  <action>
    Create `src/components/practice/PracticeHubPage.test.tsx` FIRST with these test cases:
    - Test: Page renders "Pratica Solo" section header text
    - Test: Page renders "Ao Vivo" section header text
    - Test: Pratica Solo section contains exactly 5 PracticeModeCard buttons
    - Test: Ao Vivo section contains exactly 2 PracticeModeCard buttons
    - Test: Page does NOT import or render ModeTooltip (no tooltip wrappers)
    - Test: "visual" mode (Desafio Visual) appears in Pratica Solo section, NOT Ao Vivo
    - Test: "simulation" mode appears in Ao Vivo section
    - Test: "trails" mode appears in Ao Vivo section
    - Test: All 7 modes are present across both sections with no duplicates

    Use @testing-library/react render() with MemoryRouter wrapper.
    Mock react-router-dom's useNavigate to avoid routing errors in tests.
    Access sections by finding text "Pratica Solo" / "Ao Vivo" and traversing to their parent section elements.

    Then rewrite `src/components/practice/PracticeHubPage.tsx`:

    1. Remove ModeTooltip import (per D-05)
    2. Remove ModeCard import
    3. Add import: `import { PracticeModeCard } from '../shared/PracticeModeCard'`
    4. Keep existing imports: `useNavigate` from react-router-dom, mode arrays from config/modes
    5. Define inline mode grouping (per D-07, simpler than modifying modes.ts):
       ```typescript
       const soloModes: readonly PracticeMode[] = [
         ...exerciseModes, // phrases, texts, situations, scripts (4)
         conversationModes.find(m => m.id === 'visual')!, // visual (1) = 5 total
       ];
       const liveModes: readonly PracticeMode[] = [
         conversationModes.find(m => m.id === 'simulation')!, // simulation (1)
         trailsMode, // trails (1) = 2 total
       ];
       ```
    6. Page layout: `<div className="space-y-8 pb-20">` (unchanged outer wrapper)
    7. Keep page header unchanged: h1 "Praticar" + subtitle "Escolha como quer praticar hoje"
    8. Section 1 "Pratica Solo":
       - Section header (per D-08, using bg-primary for neutral dot):
         `<div className="flex items-center gap-2 mb-4"><div className="w-2 h-2 rounded-full bg-primary" /><span className="text-xs font-semibold uppercase tracking-wider text-muted-foreground">Pratica Solo</span></div>`
       - Card container: `<div className="flex flex-col gap-3">` (gap-3 = 12px per UI-SPEC)
       - Map soloModes to PracticeModeCard with onClick={() => navigate(mode.to)}
    9. Section 2 "Ao Vivo":
       - Same section header pattern with "Ao Vivo"
       - Same card container pattern
       - Map liveModes to PracticeModeCard
    10. Remove all ModeTooltip wrappers and their extra <div> wrappers
    11. Keep named export: `export function PracticeHubPage()` (required by lazy loading in App.tsx)
    12. Note: section header uses `mb-4` (changed from `mb-3` in original) per UI-SPEC spacing specification

    CRITICAL: The `visual` mode is in `conversationModes` array but belongs in "Pratica Solo" section (per D-07, Pitfall 4 from RESEARCH.md). The inline grouping above handles this correctly.
    CRITICAL: Do NOT add soloModes/liveModes exports to modes.ts. Inline grouping is simpler and avoids modifying a config file that other consumers depend on.
  </action>
  <verify>
    <automated>npx vitest run src/components/practice/PracticeHubPage.test.tsx src/components/shared/PracticeModeCard.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - PracticeHubPage.tsx no longer imports ModeTooltip
    - PracticeHubPage.tsx no longer imports ModeCard
    - PracticeHubPage.tsx imports PracticeModeCard
    - Two sections: "Pratica Solo" (5 cards) and "Ao Vivo" (2 cards)
    - "visual" mode is in Pratica Solo, "simulation" in Ao Vivo, "trails" in Ao Vivo
    - All 7 modes rendered with no duplicates
    - All PracticeHubPage tests pass
    - All PracticeModeCard tests still pass (no regression)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of Praticar redesign</name>
  <files>none (verification only)</files>
  <read_first>
    src/components/shared/PracticeModeCard.tsx
    src/components/practice/PracticeHubPage.tsx
  </read_first>
  <acceptance_criteria>
    - npx vite --port 5173 --host starts without errors
    - Praticar page loads at /practice with 2 visible sections
    - All 7 cards render with image banners, labels, descriptions, and example text
    - Tab navigation cycles through all 7 cards with visible focus rings
    - Card hover produces image scale + shadow lift effect
    - Praticar cards are visibly taller than Trilhas PathCards
  </acceptance_criteria>
  <action>
    Human visual and interactive verification of the Praticar page redesign. No code changes in this task.
  </action>
  <what-built>
    Complete Praticar page redesign: 2-section vertical layout with image-banner cards.
    7 practice mode cards split into "Pratica Solo" (5 modes) and "Ao Vivo" (2 modes).
    Each card has an h-40 image banner on top, label/description/example text below.
    Cards use gradient+icon fallback when images fail. All cards are keyboard accessible.
  </what-built>
  <how-to-verify>
    1. Run `npx vite --port 5173 --host` to start dev server
    2. Navigate to the Praticar page (click "Praticar" in sidebar)
    3. Verify visual layout:
       - Page shows two sections: "Pratica Solo" at top, "Ao Vivo" below
       - Each card is full-width with an image banner on top (taller than PathCards on Trilhas page)
       - Card content shows label (colored), description, and italic example text below
       - No tooltips appear -- all info visible at a glance
    4. Verify image fallbacks:
       - If any mode images are missing, verify gradient background with mode icon appears instead of broken image
    5. Verify keyboard accessibility:
       - Press Tab to cycle through cards -- each card should show a visible focus ring
       - Press Enter or Space on a focused card -- should navigate to that mode's route
       - All 7 cards should be tabbable in order
    6. Verify visual distinction from Trilhas:
       - Navigate to Trilhas page and compare card heights -- Praticar cards should be visibly taller (h-40 vs h-32)
       - Praticar uses single column; Trilhas uses grid
    7. Verify hover effects:
       - Hover over a card -- image should scale up subtly, card should lift with shadow
  </how-to-verify>
  <verify>
    <automated>npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>
    Human confirms the Praticar page redesign is visually correct: 2 sections, image-banner cards, keyboard accessible, visually distinct from Trilhas, hover effects working.
  </done>
  <resume-signal>Type "approved" to confirm the redesign looks correct, or describe any visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. All tests pass: `npx vitest run 2>&1` exits 0
2. No TypeScript errors: `npx tsc --noEmit 2>&1` exits 0
3. No lint errors: `npx eslint src/components/shared/PracticeModeCard.tsx src/components/practice/PracticeHubPage.tsx 2>&1`
4. ModeTooltip is no longer imported by PracticeHubPage: `grep -c "ModeTooltip" src/components/practice/PracticeHubPage.tsx` returns 0
5. PracticeModeCard is imported: `grep "PracticeModeCard" src/components/practice/PracticeHubPage.tsx` returns 1+ matches
6. h-40 class present on image banner: `grep "h-40" src/components/shared/PracticeModeCard.tsx` returns 1+ matches
7. aria-label present on button: `grep "aria-label" src/components/shared/PracticeModeCard.tsx` returns 1+ matches
</verification>

<success_criteria>
1. PracticeHubPage renders 2 sections ("Pratica Solo" with 5 cards, "Ao Vivo" with 2 cards) -- VIS-01
2. Cards use h-40 image banners, visibly taller than PathCard h-32 -- VIS-02
3. Cards are <button> elements with aria-label, focus-visible rings, keyboard-activatable -- VIS-03
4. ModeTooltip removed from PracticeHubPage -- per D-05
5. All vitest tests pass (PracticeModeCard + PracticeHubPage + existing suite)
6. Human confirms visual quality matches design intent
</success_criteria>

<output>
After completion, create `.planning/phases/06-praticar-redesign/06-01-SUMMARY.md`
</output>
