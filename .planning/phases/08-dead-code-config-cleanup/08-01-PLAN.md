---
phase: 08-dead-code-config-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vite.config.ts
  - vitest.smoke.config.ts
  - nginx.conf
autonomous: true
requirements:
  - DC-01
  - DC-02

must_haves:
  truths:
    - "vite.config.ts contains zero references to /api/groq"
    - "vitest.smoke.config.ts contains zero references to /api/groq"
    - "nginx.conf contains zero references to /api/groq"
    - "vite.config.ts coverage.include does not reference openaiRealtimeLive.ts"
    - "vite.config.ts has no empty server: {} key"
    - "App builds and tests pass after cleanup"
  artifacts:
    - path: "vite.config.ts"
      provides: "Clean Vite config with no dead proxy and no stale coverage ref"
    - path: "vitest.smoke.config.ts"
      provides: "Clean smoke test config with no dead proxy"
    - path: "nginx.conf"
      provides: "Production config with no dead Groq proxy block"
  key_links:
    - from: "vite.config.ts"
      to: "vitest run"
      via: "build and test commands"
      pattern: "npx vitest run.*exits 0"
---

<objective>
Remove dead Groq proxy configuration and stale coverage references from build/deployment config files.

Purpose: Clean foundation for subsequent v1.2 phases (Model Catalog, Edge Function Modularization). The `/api/groq` proxy is vestigial — Groq API calls now route through the Supabase Edge Function `ai-proxy`. The `openaiRealtimeLive.ts` file was deleted in v1.1 (Phase 7) but its coverage reference remains.

Output: Three config files cleaned of dead code, build and tests still pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-dead-code-config-cleanup/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead /api/groq proxy from all config files</name>
  <files>vite.config.ts, vitest.smoke.config.ts, nginx.conf</files>
  <read_first>
    vite.config.ts — current file with proxy block (lines 36-44) and server key
    vitest.smoke.config.ts — current file with identical proxy block (lines 16-24)
    nginx.conf — current file with /api/groq/ location block (lines 8-14)
  </read_first>
  <action>
Per D-01: Remove the dead `/api/groq` proxy configuration from all three files. Zero consumers confirmed (grep of `src/` returns no hits for `/api/groq`).

**vite.config.ts — delete lines 36-44 (the entire `server: { proxy: { ... } }` block).**

Per D-03: Remove the entire `server:` key since it becomes empty after proxy removal. The closing `}` on line 45 of the `defineConfig({})` call should directly follow the `test: { ... }` closing brace with no empty `server:` key in between.

The result should look like:
```
export default defineConfig({
  plugins: [react(), tailwindcss()],
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: './src/test/setup.ts',
    exclude: [...configDefaults.exclude, '**/*.smoke.test.ts'],
    coverage: {
      provider: 'v8',
      reporter: ['text', 'html'],
      include: [
        'src/services/openai.ts',
        'src/services/geminiLive.ts',
        'src/services/openaiRealtimeLive.ts',
      ],
      thresholds: {
        statements: 35,
        branches: 25,
        functions: 30,
        lines: 40,
      },
    },
  },
})
```
Note: The `openaiRealtimeLive.ts` line stays for now — Task 2 removes it.

Also remove the comment on line 10 (`// VITE_GROQ_API_KEY=your-groq-key (optional, for development)`) since the Groq proxy it served is now dead. Keep all other env var comments (lines 6-9).

**vitest.smoke.config.ts — delete lines 16-24 (the entire `server: { proxy: { ... } }` block).**

The result should look like:
```
import { defineConfig } from 'vitest/config'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'

export default defineConfig({
  plugins: [react(), tailwindcss()],
  test: {
    environment: 'jsdom',
    globals: true,
    include: ['src/**/*.smoke.test.ts'],
    setupFiles: ['./src/test/setup.ts', './src/test/smoke/setup.ts'],
    fileParallelism: false,
    testTimeout: 120000,
    hookTimeout: 120000,
  },
})
```

**nginx.conf — delete lines 8-14 (the entire `location /api/groq/ { ... }` block) and the comment on line 8.**

The result should look like:
```
server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    include /etc/nginx/mime.types;
    index index.html;

    # SPA fallback — all routes go to index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Cache static assets
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
}
```
  </action>
  <acceptance_criteria>
    - `grep -c '/api/groq' vite.config.ts` returns 0
    - `grep -c '/api/groq' vitest.smoke.config.ts` returns 0
    - `grep -c '/api/groq' nginx.conf` returns 0
    - `grep -c 'server:' vite.config.ts` returns 0 (no empty server key per D-03)
    - `grep -c 'server:' vitest.smoke.config.ts` returns 0 (no empty server key per D-03)
    - `grep -c 'proxy' nginx.conf` returns 0
    - vite.config.ts does NOT contain the line `// VITE_GROQ_API_KEY=your-groq-key`
  </acceptance_criteria>
  <verify>
    <automated>grep -r '/api/groq' vite.config.ts vitest.smoke.config.ts nginx.conf; echo "EXIT:$?"</automated>
  </verify>
  <done>Zero references to /api/groq across all three config files. No empty server: keys remain. Groq env var comment removed from vite.config.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Remove stale openaiRealtimeLive.ts coverage reference</name>
  <files>vite.config.ts</files>
  <read_first>
    vite.config.ts — must read after Task 1 edits to see current state of coverage.include array
  </read_first>
  <action>
Per D-02: Remove only the stale `openaiRealtimeLive.ts` reference from the coverage `include` array in `vite.config.ts`. The file was deleted in v1.1 (Phase 7) — only the coverage reference remains.

In the `coverage.include` array, delete the line:
```
'src/services/openaiRealtimeLive.ts',
```

Keep the remaining two entries and all other coverage config (provider, reporter, thresholds) exactly as-is. No expansion or changes to thresholds.

The `coverage.include` array should become:
```
include: [
  'src/services/openai.ts',
  'src/services/geminiLive.ts',
],
```
  </action>
  <acceptance_criteria>
    - `grep -c 'openaiRealtimeLive' vite.config.ts` returns 0
    - `grep -c 'openai.ts' vite.config.ts` returns 1 (remaining entry preserved)
    - `grep -c 'geminiLive.ts' vite.config.ts` returns 1 (remaining entry preserved)
    - `npx vitest run --coverage 2>&1 | tail -5` shows no warnings about missing files
  </acceptance_criteria>
  <verify>
    <automated>grep 'openaiRealtimeLive' vite.config.ts; echo "EXIT:$?"</automated>
  </verify>
  <done>coverage.include in vite.config.ts references only existing files (openai.ts, geminiLive.ts). Coverage runs without warnings about missing files.</done>
</task>

</tasks>

<verification>
1. `grep -r '/api/groq' vite.config.ts vitest.smoke.config.ts nginx.conf` returns no output (zero hits)
2. `grep 'openaiRealtimeLive' vite.config.ts` returns no output (zero hits)
3. `grep 'server:' vite.config.ts` returns no output (empty key removed per D-03)
4. `npx vite build` succeeds (build still works)
5. `npx vitest run` succeeds (tests still pass)
</verification>

<success_criteria>
- vite.config.ts, vitest.smoke.config.ts, and nginx.conf contain zero references to /api/groq
- vite.config.ts coverage.include has no reference to openaiRealtimeLive.ts
- vite.config.ts has no empty server: {} key
- `npx vite build` exits 0
- `npx vitest run` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/08-dead-code-config-cleanup/08-01-SUMMARY.md`
</output>
