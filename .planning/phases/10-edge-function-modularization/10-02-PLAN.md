---
phase: 10-edge-function-modularization
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - supabase/functions/ai-proxy/providers/openai.ts
  - supabase/functions/ai-proxy/providers/gemini.ts
  - supabase/functions/ai-proxy/providers/groq.ts
  - supabase/functions/ai-proxy/providers/openrouter.ts
  - supabase/functions/ai-proxy/providers/vertex.ts
autonomous: true
requirements:
  - EF-01
  - EF-03

must_haves:
  truths:
    - "Each provider module exports its action functions (chat, tts, stt, image where applicable)"
    - "Provider function signatures match the original exactly -- same parameters, same return types"
    - "No behavior changes -- request/response shapes are identical to original monolith"
  artifacts:
    - path: "supabase/functions/ai-proxy/providers/openai.ts"
      provides: "OpenAI chat, TTS, STT, and image generation"
      contains: "export async function chat"
    - path: "supabase/functions/ai-proxy/providers/gemini.ts"
      provides: "Gemini chat, TTS, STT, and image generation with dynamic imports"
      contains: "export async function chat"
    - path: "supabase/functions/ai-proxy/providers/groq.ts"
      provides: "Groq chat, TTS, and STT"
      contains: "export async function chat"
    - path: "supabase/functions/ai-proxy/providers/openrouter.ts"
      provides: "OpenRouter chat, TTS, STT, and image generation"
      contains: "export async function chat"
    - path: "supabase/functions/ai-proxy/providers/vertex.ts"
      provides: "Vertex AI auth, config, chat, chatWithImage, TTS, STT, and image"
      contains: "export async function chat"
  key_links:
    - from: "supabase/functions/ai-proxy/providers/gemini.ts"
      to: "supabase/functions/ai-proxy/utils.ts"
      via: "import { pcm16ToWav } from '../utils.ts'"
      pattern: "import.*utils\\.ts"
    - from: "supabase/functions/ai-proxy/providers/vertex.ts"
      to: "supabase/functions/ai-proxy/utils.ts"
      via: "import { pcm16ToWav, str2ab } from '../utils.ts'"
      pattern: "import.*utils\\.ts"
---

<objective>
Extract all 5 provider modules from the ai-proxy monolith.

Purpose: Each provider module encapsulates the API calls for a single AI provider. These are the meat of the function and the primary maintainability win.
Output: 5 provider files in supabase/functions/ai-proxy/providers/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-edge-function-modularization/10-RESEARCH.md

Original source: supabase/functions/ai-proxy/index.ts (1364 lines)

Line ranges to extract:
- providers/openai.ts: lines 214-238 (openaiChat) + 298-321 (openaiTTS) + 382-406 (openaiSTT) + 927-955 (openaiImage)
- providers/gemini.ts: lines 243-264 (geminiChat) + 326-349 (geminiTTS) + 411-428 (geminiSTT) + 960-1035 (geminiImage)
- providers/groq.ts: lines 269-293 (groqChat) + 354-377 (groqTTS) + 433-457 (groqSTT)
- providers/openrouter.ts: lines 466-492 (openrouterChat) + 498-520 (openrouterTTS) + 526-554 (openrouterSTT) + 560-599 (openrouterImage)
- providers/vertex.ts: lines 609-659 (getVertexAccessToken) + 665-685 (getVertexConfig) + 690-713 (vertexChat) + 718-756 (vertexChatWithImage) + 761-794 (vertexTTS) + 799-827 (vertexSTT) + 832-905 (vertexImage)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create OpenAI, Gemini, and Groq provider modules</name>
  <files>supabase/functions/ai-proxy/providers/openai.ts, supabase/functions/ai-proxy/providers/gemini.ts, supabase/functions/ai-proxy/providers/groq.ts</files>
  <read_first>supabase/functions/ai-proxy/index.ts</read_first>
  <action>
Create 3 provider modules in the providers/ subdirectory. Create the providers/ directory first.

**providers/openai.ts** -- Extract 4 functions, rename to shorter names since the module itself provides namespace:
- `export async function chat(apiKey: string, model: string, systemPrompt: string, userMessage: string, temperature = 0.8): Promise<string>` -- from lines 214-238
- `export async function tts(apiKey: string, text: string, voice: string, model: string): Promise<string>` -- from lines 298-321
- `export async function stt(apiKey: string, audioBase64: string, mimeType: string, model: string): Promise<string>` -- from lines 382-406
- `export async function image(apiKey: string, prompt: string, model: string, options: Record<string, unknown>): Promise<string>` -- from lines 927-955
- No sibling imports needed -- these are pure fetch calls

**providers/gemini.ts** -- Extract 4 functions:
- `export async function chat(apiKey: string, model: string, systemPrompt: string, userMessage: string): Promise<string>` -- from lines 243-264
- `export async function tts(apiKey: string, text: string, voice: string, model: string): Promise<string>` -- from lines 326-349. IMPORTANT: This function uses `pcm16ToWav` -- import from `../utils.ts`. Also uses dynamic `await import('https://esm.sh/@google/generative-ai@0.21.0')` -- preserve exactly.
- `export async function stt(apiKey: string, audioBase64: string, mimeType: string): Promise<string>` -- from lines 411-428. Uses dynamic import -- preserve exactly.
- `export async function image(apiKey: string, prompt: string, model: string, options: Record<string, unknown>): Promise<string>` -- from lines 960-1035
- Import: `import { pcm16ToWav } from '../utils.ts'`

**providers/groq.ts** -- Extract 3 functions (Groq has no image generation):
- `export async function chat(apiKey: string, model: string, systemPrompt: string, userMessage: string): Promise<string>` -- from lines 269-293
- `export async function tts(apiKey: string, text: string, voice: string, model: string): Promise<string>` -- from lines 354-377
- `export async function stt(apiKey: string, audioBase64: string, mimeType: string, model: string): Promise<string>` -- from lines 433-457
- No sibling imports needed

CRITICAL: Copy the function BODIES verbatim. Do not rename parameters, restructure logic, or change error messages. The only change is removing the provider name prefix from function names (e.g., `openaiChat` becomes `chat` in openai.ts). All imports use explicit `.ts` extensions.
  </action>
  <verify>
    <automated>grep -c "export async function chat" supabase/functions/ai-proxy/providers/openai.ts && grep -c "export async function chat" supabase/functions/ai-proxy/providers/gemini.ts && grep -c "export async function chat" supabase/functions/ai-proxy/providers/groq.ts && grep -c "import.*utils\\.ts" supabase/functions/ai-proxy/providers/gemini.ts</automated>
  </verify>
  <done>3 provider modules created with all exported functions matching original signatures. Gemini imports pcm16ToWav from ../utils.ts. Dynamic imports for @google/generative-ai preserved exactly.</done>
</task>

<task type="auto">
  <name>Task 2: Create OpenRouter and Vertex AI provider modules</name>
  <files>supabase/functions/ai-proxy/providers/openrouter.ts, supabase/functions/ai-proxy/providers/vertex.ts</files>
  <read_first>supabase/functions/ai-proxy/index.ts</read_first>
  <action>
Create 2 remaining provider modules.

**providers/openrouter.ts** -- Extract 4 functions:
- `export async function chat(apiKey: string, model: string, systemPrompt: string, userMessage: string, temperature = 0.8): Promise<string>` -- from lines 466-492. Note the extra headers: `HTTP-Referer: 'https://speaklab.app'` and `X-Title: 'SpeakLab'` -- preserve exactly.
- `export async function tts(apiKey: string, text: string, voice: string, model: string): Promise<string>` -- from lines 498-520
- `export async function stt(apiKey: string, audioBase64: string, mimeType: string, model: string): Promise<string>` -- from lines 526-554. Uses chat completions with input_audio content type, not a transcription endpoint -- preserve exactly.
- `export async function image(apiKey: string, prompt: string, model: string): Promise<string>` -- from lines 560-599. Note: no `options` parameter unlike other providers.
- No sibling imports needed

**providers/vertex.ts** -- Extract 7 functions (largest module, ~310 lines):
- `export async function getAccessToken(): Promise<string>` -- from lines 609-659. Uses `str2ab` and `crypto.subtle` for JWT signing -- import str2ab from `../utils.ts`. Reads `Deno.env.get('VERTEX_SERVICE_ACCOUNT_KEY')`.
- `export async function getConfig(supabaseClient: any, userId: string): Promise<{ projectId: string; region: string }>` -- from lines 665-685. IMPORTANT: Accept `supabaseClient` as parameter (not closure-captured), same pattern as api-keys.ts.
- `export async function chat(accessToken: string, projectId: string, region: string, model: string, systemPrompt: string, userMessage: string): Promise<string>` -- from lines 690-713
- `export async function chatWithImage(accessToken: string, projectId: string, region: string, model: string, systemPrompt: string, imageBase64: string, imageMimeType: string): Promise<string>` -- from lines 718-756
- `export async function tts(accessToken: string, projectId: string, region: string, model: string, text: string, voice: string): Promise<string>` -- from lines 761-794. Uses `pcm16ToWav` -- import from `../utils.ts`.
- `export async function stt(accessToken: string, projectId: string, region: string, model: string, audioBase64: string, mimeType: string): Promise<string>` -- from lines 799-827
- `export async function image(accessToken: string, projectId: string, region: string, model: string, prompt: string, options: Record<string, unknown>): Promise<string>` -- from lines 832-905
- Imports: `import { pcm16ToWav, str2ab } from '../utils.ts'`

CRITICAL: Vertex's `getAccessToken` reads Deno.env directly -- that's fine, no change needed. But `getConfig` must accept supabaseClient as parameter instead of using a closure variable. Copy all function bodies verbatim.
  </action>
  <verify>
    <automated>grep -c "export async function chat" supabase/functions/ai-proxy/providers/openrouter.ts && grep -c "export async function getAccessToken" supabase/functions/ai-proxy/providers/vertex.ts && grep -c "export async function getConfig" supabase/functions/ai-proxy/providers/vertex.ts && grep -c "import.*utils\\.ts" supabase/functions/ai-proxy/providers/vertex.ts</automated>
  </verify>
  <done>OpenRouter module exports chat/tts/stt/image with SpeakLab headers preserved. Vertex module exports 7 functions including auth and config. Vertex's getConfig accepts supabaseClient as parameter. Both import from ../utils.ts with .ts extension.</done>
</task>

</tasks>

<verification>
All 5 provider modules exist with correct exports. No function body logic changed. Dynamic imports for Gemini SDK preserved. Vertex supabaseClient passed as parameter instead of closure-captured.
</verification>

<success_criteria>
- providers/openai.ts: exports chat, tts, stt, image (4 functions)
- providers/gemini.ts: exports chat, tts, stt, image (4 functions), imports pcm16ToWav from ../utils.ts
- providers/groq.ts: exports chat, tts, stt (3 functions)
- providers/openrouter.ts: exports chat, tts, stt, image (4 functions) with SpeakLab headers
- providers/vertex.ts: exports getAccessToken, getConfig, chat, chatWithImage, tts, stt, image (7 functions), imports from ../utils.ts
- All imports use explicit .ts extensions
</success_criteria>

<output>
After completion, create `.planning/phases/10-edge-function-modularization/10-02-SUMMARY.md`
</output>
