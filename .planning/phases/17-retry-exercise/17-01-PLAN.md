---
phase: 17-retry-exercise
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/discovery/ExerciseMode.tsx
  - src/components/discovery/ImageMode.tsx
autonomous: true
requirements:
  - RETRY-01
  - RETRY-02
  - RETRY-03
  - RETRY-04

must_haves:
  truths:
    - "After exercise evaluation, user sees 3 buttons: Tentar Novamente (retry same), Novo Exercicio (new), Voltar ao Hub (exit)"
    - "Retry clears evaluation/audio but preserves the exercise prompt"
    - "New Exercise clears all state and returns to setup"
    - "Voltar ao Hub navigates to /practice"
    - "After image exercise evaluation, same 3-button pattern works preserving imageUrl + question"
  artifacts:
    - path: "src/components/discovery/ExerciseMode.tsx"
      provides: "3-button post-evaluation layout with retrySame function"
      contains: "retrySame"
    - path: "src/components/discovery/ImageMode.tsx"
      provides: "3-button post-evaluation layout with retrySame function"
      contains: "retrySame"
  key_links:
    - from: "src/components/discovery/ExerciseMode.tsx"
      to: "/practice"
      via: "navigate('/practice')"
      pattern: "navigate.*practice"
    - from: "src/components/discovery/ImageMode.tsx"
      to: "/practice"
      via: "navigate('/practice')"
      pattern: "navigate.*practice"
---

<objective>
Add retry-same-exercise and post-exercise navigation to ExerciseMode and ImageMode.

Purpose: Currently both components offer a single "Tentar Outro" button that fully resets state. Users need: (1) retry the same prompt without regenerating, (2) generate a new exercise, (3) go back to practice hub.

Output: Modified ExerciseMode.tsx and ImageMode.tsx with 3-button post-evaluation layouts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-retry-exercise/17-RESEARCH.md
@.planning/phases/17-retry-exercise/17-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add retry + post-exercise navigation to ExerciseMode</name>
  <files>src/components/discovery/ExerciseMode.tsx</files>
  <read_first>
    src/components/discovery/ExerciseMode.tsx
    src/components/ui/Button.tsx
  </read_first>
  <action>
    Modify ExerciseMode.tsx to replace the single "Tentar Outro" button with a 3-button post-evaluation layout.

    1. Add `useNavigate` import from `react-router-dom` (it is NOT currently imported — check line 1, only `useState, useEffect` are imported from react, no react-router-dom import exists).

    2. Add icon imports: add `RotateCcw`, `ChevronLeft` to the existing lucide-react import on line 2. The current import is:
       `import { Loader2, RefreshCw, X, Sparkles, ImageIcon, Mic, ChevronLeft, ChevronRight, MessageCircle, FileText, Theater } from 'lucide-react';`
       — ChevronLeft is already imported. Add `RotateCcw` to this import list.

    3. Add `const navigate = useNavigate();` inside the `ExerciseMode` function, after the existing state declarations (after line 128, before the `config` constant on line 129).

    4. Add a `retrySame` function after the existing `reset` function (after line 224). The function body must be:
       ```
       const retrySame = () => {
         setEvaluation(null);
         setError(null);
         setSaved(false);
         setUserAudioBase64(null);
         // Keep prompt intact — user sees the same exercise prompt again
       };
       ```
       This preserves `prompt` (does NOT call setPrompt('')), unlike `reset()` which clears everything.

    5. In the evaluation section (the `if (evaluation)` block starting at line 426), replace the single button:
       ```
       <Button variant="secondary" size="lg" onClick={reset} className="w-full rounded-2xl cursor-pointer">
         <RefreshCw size={18} />
         Tentar Outro
       </Button>
       ```
       with a 3-button group:
       ```
       <div className="space-y-2">
         <Button variant="primary" size="lg" onClick={retrySame} className="w-full rounded-2xl cursor-pointer">
           <RotateCcw size={18} />
           Tentar Novamente
         </Button>
         <Button variant="secondary" size="lg" onClick={reset} className="w-full rounded-2xl cursor-pointer">
           <RefreshCw size={18} />
           Novo Exercicio
         </Button>
         <Button variant="ghost" size="lg" onClick={() => navigate('/practice')} className="w-full rounded-2xl cursor-pointer">
           <ChevronLeft size={18} />
           Voltar ao Hub
         </Button>
       </div>
       ```

    Do NOT modify the setup UI, the active session UI, or the AudioRecorder integration. Only modify the evaluation results section.
  </action>
  <verify>
    <automated>grep -c "retrySame" src/components/discovery/ExerciseMode.tsx && grep -c "navigate.*practice" src/components/discovery/ExerciseMode.tsx && grep -c "Tentar Novamente" src/components/discovery/ExerciseMode.tsx && grep -c "Novo Exercicio" src/components/discovery/ExerciseMode.tsx && grep -c "Voltar ao Hub" src/components/discovery/ExerciseMode.tsx</automated>
  </verify>
  <acceptance_criteria>
    - ExerciseMode.tsx contains `import { useNavigate }` or `useNavigate` is imported from react-router-dom
    - ExerciseMode.tsx contains `const navigate = useNavigate()`
    - ExerciseMode.tsx contains function `retrySame` that does NOT call setPrompt
    - ExerciseMode.tsx contains string "Tentar Novamente" (retry same button label)
    - ExerciseMode.tsx contains string "Novo Exercicio" (new exercise button label)
    - ExerciseMode.tsx contains string "Voltar ao Hub" (back to hub button label)
    - ExerciseMode.tsx contains `navigate('/practice')` or `navigate("/practice")`
    - The string "Tentar Outro" no longer appears in the file
    - `npx vitest run --reporter=verbose` passes with no failures
  </acceptance_criteria>
  <done>
    ExerciseMode evaluation view shows 3 buttons: "Tentar Novamente" (primary, clears evaluation preserves prompt), "Novo Exercicio" (secondary, full reset to setup), "Voltar ao Hub" (ghost, navigates to /practice). Existing test suite passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add retry + post-exercise navigation to ImageMode</name>
  <files>src/components/discovery/ImageMode.tsx</files>
  <read_first>
    src/components/discovery/ImageMode.tsx
    src/components/ui/Button.tsx
  </read_first>
  <action>
    Modify ImageMode.tsx to replace the single "Tentar Outro" button with a 3-button post-evaluation layout.

    1. Add `useNavigate` import from `react-router-dom`. The current file does NOT import useNavigate (line 1 imports from 'react', nothing from react-router-dom). Add:
       `import { useNavigate } from 'react-router-dom';`
       as a new import line after the existing imports.

    2. Add icon imports: add `RotateCcw`, `ChevronLeft` to the existing lucide-react import on line 2. Current import is:
       `import { Loader2, RefreshCw, X, ImageIcon, Sparkles } from 'lucide-react';`
       Change to:
       `import { Loader2, RefreshCw, X, ImageIcon, Sparkles, RotateCcw, ChevronLeft } from 'lucide-react';`

    3. Add `const navigate = useNavigate();` inside the `ImageMode` function, after the state declarations (after line 27 `const [tone, setTone] = useState<ConversationTone>('balanced');`).

    4. Add a `retrySame` function after the existing `reset` function (after line 103). The function body must be:
       ```
       const retrySame = () => {
         setEvaluation(null);
         setError(null);
         setSaved(false);
         setUserAudioBase64(null);
         // Keep imageUrl and question intact — user sees same image and question again
       };
       ```
       This preserves `imageUrl` and `question`, unlike `reset()` which clears everything.

    5. In the evaluation section (the `{evaluation && (` block starting at line 174), replace the single button:
       ```
       <Button variant="secondary" size="lg" onClick={reset} className="w-full rounded-2xl">
         <RefreshCw size={18} />
         Tentar Outro
       </Button>
       ```
       with a 3-button group:
       ```
       <div className="space-y-2">
         <Button variant="primary" size="lg" onClick={retrySame} className="w-full rounded-2xl cursor-pointer">
           <RotateCcw size={18} />
           Tentar Novamente
         </Button>
         <Button variant="secondary" size="lg" onClick={reset} className="w-full rounded-2xl cursor-pointer">
           <RefreshCw size={18} />
           Novo Exercicio
         </Button>
         <Button variant="ghost" size="lg" onClick={() => navigate('/practice')} className="w-full rounded-2xl cursor-pointer">
           <ChevronLeft size={18} />
           Voltar ao Hub
         </Button>
       </div>
       ```

    Do NOT modify the image generation, loading skeleton, or AudioRecorder integration. Only modify the evaluation results section.
  </action>
  <verify>
    <automated>grep -c "retrySame" src/components/discovery/ImageMode.tsx && grep -c "navigate.*practice" src/components/discovery/ImageMode.tsx && grep -c "Tentar Novamente" src/components/discovery/ImageMode.tsx && grep -c "Novo Exercicio" src/components/discovery/ImageMode.tsx && grep -c "Voltar ao Hub" src/components/discovery/ImageMode.tsx</automated>
  </verify>
  <acceptance_criteria>
    - ImageMode.tsx contains `import { useNavigate }` from react-router-dom
    - ImageMode.tsx contains `const navigate = useNavigate()`
    - ImageMode.tsx contains function `retrySame` that does NOT call setImageUrl or setQuestion
    - ImageMode.tsx contains string "Tentar Novamente" (retry same button label)
    - ImageMode.tsx contains string "Novo Exercicio" (new exercise button label)
    - ImageMode.tsx contains string "Voltar ao Hub" (back to hub button label)
    - ImageMode.tsx contains `navigate('/practice')` or `navigate("/practice")`
    - The string "Tentar Outro" no longer appears in the file
    - `npx vitest run --reporter=verbose` passes with no failures
  </acceptance_criteria>
  <done>
    ImageMode evaluation view shows 3 buttons: "Tentar Novamente" (primary, clears evaluation preserves image+question), "Novo Exercicio" (secondary, full reset), "Voltar ao Hub" (ghost, navigates to /practice). Existing test suite passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client->router | navigate('/practice') call — internal routing only, no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | S | ExerciseMode/ImageMode | accept | No user input involved — buttons trigger hardcoded state resets and navigation |
| T-17-02 | I | retrySame/reset | accept | State management is local component state, no data crosses trust boundary |

No high-severity threats. All changes are client-side UI state transitions with no external input handling, no API calls, no data persistence changes.
</threat_model>

<verification>
- Both files compile: `npx tsc --noEmit` passes
- Existing test suite green: `npx vitest run --reporter=verbose`
- No string "Tentar Outro" in either file: `grep -r "Tentar Outro" src/components/discovery/` returns nothing
- Both files contain retrySame function: `grep -l "retrySame" src/components/discovery/ExerciseMode.tsx src/components/discovery/ImageMode.tsx`
</verification>

<success_criteria>
- ExerciseMode.tsx has 3-button post-evaluation layout (Tentar Novamente / Novo Exercicio / Voltar ao Hub)
- ImageMode.tsx has identical 3-button post-evaluation layout
- retrySame preserves prompt/image, reset clears everything
- Voltar ao Hub navigates to /practice in both components
- All existing tests pass
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/17-retry-exercise/17-01-SUMMARY.md`
</output>
