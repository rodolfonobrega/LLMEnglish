---
phase: 17-retry-exercise
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/live-roleplay/LiveRoleplayPage.tsx
  - src/components/live-roleplay/ConversationAnalysis.tsx
autonomous: true
requirements:
  - RETRY-05
  - RETRY-06
  - RETRY-07

must_haves:
  truths:
    - "After live roleplay analysis, user can retry the same scenario (same scenario, clear turns, re-enter conversation)"
    - "Existing 'Nova Conversa' button still works (goes to setup)"
    - "ConversationAnalysis receives and uses an onRetry callback"
  artifacts:
    - path: "src/components/live-roleplay/LiveRoleplayPage.tsx"
      provides: "handleRetryScenario callback that preserves scenario, clears turns"
      contains: "handleRetryScenario"
    - path: "src/components/live-roleplay/ConversationAnalysis.tsx"
      provides: "onRetry prop and retry button in action bar"
      contains: "onRetry"
  key_links:
    - from: "src/components/live-roleplay/LiveRoleplayPage.tsx"
      to: "src/components/live-roleplay/ConversationAnalysis.tsx"
      via: "onRetry={handleRetryScenario} prop"
      pattern: "onRetry"
    - from: "src/components/live-roleplay/LiveRoleplayPage.tsx"
      to: "setPhase"
      via: "handleRetryScenario sets phase to 'conversation'"
      pattern: "setPhase.*conversation"
---

<objective>
Add retry-same-scenario option to live roleplay flow.

Purpose: Currently after a live roleplay analysis, the "Nova Conversa" button goes all the way back to setup, requiring the user to configure a new scenario. Users need a way to retry the SAME scenario (same roleplay context, fresh conversation) without going through setup again.

Output: Modified LiveRoleplayPage.tsx with handleRetryScenario callback, and ConversationAnalysis.tsx with onRetry prop and "Tentar Novamente" button.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-retry-exercise/17-RESEARCH.md
@.planning/phases/17-retry-exercise/17-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add retry-same-scenario to LiveRoleplayPage and ConversationAnalysis</name>
  <files>src/components/live-roleplay/LiveRoleplayPage.tsx, src/components/live-roleplay/ConversationAnalysis.tsx</files>
  <read_first>
    src/components/live-roleplay/LiveRoleplayPage.tsx
    src/components/live-roleplay/ConversationAnalysis.tsx
    src/components/ui/Button.tsx
  </read_first>
  <action>
    Modify two files to add retry-same-scenario functionality to the live roleplay flow.

    **File 1: LiveRoleplayPage.tsx**

    1. Add a new handler function `handleRetryScenario` after the existing `handleExit` function (after line 32). The function body must be:
       ```
       const handleRetryScenario = () => {
         setTurns([]);
         setPhase('conversation');
         // Keep scenario intact — re-enters LiveSession with same scenario
       };
       ```

    2. In the JSX, pass `onRetry` to ConversationAnalysis. Change line 60-64:
       ```
       <ConversationAnalysis
         scenario={scenario}
         turns={turns}
         onReset={handleExit}
       />
       ```
       to:
       ```
       <ConversationAnalysis
         scenario={scenario}
         turns={turns}
         onReset={handleExit}
         onRetry={handleRetryScenario}
       />
       ```

    **File 2: ConversationAnalysis.tsx**

    1. Update the `ConversationAnalysisProps` interface (lines 24-28). Add `onRetry` as an optional callback:
       ```
       interface ConversationAnalysisProps {
         scenario: LiveScenario;
         turns: ConversationTurn[];
         onReset: () => void;
         onRetry?: () => void;
       }
       ```

    2. Update the function signature to destructure `onRetry`:
       Change line 55:
       `export function ConversationAnalysis({ scenario, turns, onReset }: ConversationAnalysisProps) {`
       to:
       `export function ConversationAnalysis({ scenario, turns, onReset, onRetry }: ConversationAnalysisProps) {`

    3. In the Actions section at the bottom of the component (lines 423-443), modify the button group. The current layout is two buttons side by side:
       ```
       <div className="flex gap-3">
         <Button size="lg" onClick={() => navigate('/history')} variant="outline" className="flex-1 text-base font-bold py-6 rounded-xl">
           <Clock size={18} className="mr-2" />
           Historico
         </Button>
         <Button size="lg" onClick={onReset} variant="default" className="flex-1 text-base font-bold py-6 rounded-xl shadow-lg hover:shadow-xl transition-all">
           <RotateCcw size={18} className="mr-2" />
           Nova Conversa
         </Button>
       </div>
       ```

       Replace with a vertical 3-button layout that matches the UI-SPEC contract:
       ```
       <div className="space-y-2">
         <Button variant="primary" size="lg" onClick={onRetry} className="w-full rounded-2xl cursor-pointer">
           <RotateCcw size={18} />
           Tentar Novamente
         </Button>
         <Button variant="secondary" size="lg" onClick={onReset} className="w-full rounded-2xl cursor-pointer">
           <Sparkles size={18} />
           Novo Cenario
         </Button>
         <Button variant="ghost" size="lg" onClick={() => navigate('/history')} className="w-full rounded-2xl cursor-pointer">
           <Clock size={18} />
           Ver Historico
         </Button>
       </div>
       ```

       Note the changes:
       - "Tentar Novamente" (primary, RotateCcw icon) calls onRetry — retries same scenario
       - "Nova Conversa" renamed to "Novo Cenario" (secondary, Sparkles icon) calls onReset — goes to setup
       - "Historico" renamed to "Ver Historico" (ghost, Clock icon) navigates to /history
       - Layout changed from `flex gap-3` side-by-side to `space-y-2` vertical stack
       - All buttons use `w-full rounded-2xl cursor-pointer` matching ExerciseMode/ImageMode pattern
       - Icon className `className="mr-2"` removed from all icons (Button component already has `gap-2` in base styles)

    4. Verify `Sparkles` is already imported in ConversationAnalysis.tsx (line 12 shows it is: `import { Loader2, Volume2, Play, Square, RotateCcw, CheckCircle2, AlertTriangle, Sparkles, Clock } from 'lucide-react';`). It is present — no import change needed.

    5. Verify `navigate` is already available in ConversationAnalysis (line 13 shows `import { useNavigate } from 'react-router-dom';` and line 56 shows `const navigate = useNavigate();`). It is present — no change needed.
  </action>
  <verify>
    <automated>grep -c "handleRetryScenario" src/components/live-roleplay/LiveRoleplayPage.tsx && grep -c "onRetry" src/components/live-roleplay/ConversationAnalysis.tsx && grep -c "Tentar Novamente" src/components/live-roleplay/ConversationAnalysis.tsx && grep -c "Novo Cenario" src/components/live-roleplay/ConversationAnalysis.tsx && grep -c "Ver Historico" src/components/live-roleplay/ConversationAnalysis.tsx</automated>
  </verify>
  <acceptance_criteria>
    - LiveRoleplayPage.tsx contains function `handleRetryScenario` that sets turns to [] and phase to 'conversation' without clearing scenario
    - LiveRoleplayPage.tsx passes `onRetry={handleRetryScenario}` to ConversationAnalysis
    - ConversationAnalysis.tsx interface contains `onRetry?: () => void`
    - ConversationAnalysis.tsx destructures `onRetry` from props
    - ConversationAnalysis.tsx contains string "Tentar Novamente" (retry button calling onRetry)
    - ConversationAnalysis.tsx contains string "Novo Cenario" (new scenario button calling onReset)
    - ConversationAnalysis.tsx contains string "Ver Historico" (history button navigating to /history)
    - The string "Nova Conversa" no longer appears in ConversationAnalysis.tsx
    - The string "Historico" as a standalone button label no longer appears (replaced by "Ver Historico")
    - `npx vitest run --reporter=verbose` passes with no failures
  </acceptance_criteria>
  <done>
    LiveRoleplayPage has handleRetryScenario that preserves scenario and re-enters conversation phase. ConversationAnalysis shows 3 buttons: "Tentar Novamente" (retry same scenario), "Novo Cenario" (go to setup), "Ver Historico" (view history). onRetry prop threaded from LiveRoleplayPage through ConversationAnalysis. All existing tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client->router | navigate('/history') call — internal routing only |
| component->component | onRetry callback passed as prop — internal React prop drilling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03 | S | ConversationAnalysis | accept | No user input handling — buttons trigger callbacks with no external data |
| T-17-04 | I | LiveRoleplayPage | accept | State management is local, scenario is preserved from prior validated setup |

No high-severity threats. Changes are internal component state transitions and prop threading.
</threat_model>

<verification>
- Both files compile: `npx tsc --noEmit` passes
- Existing test suite green: `npx vitest run --reporter=verbose`
- onRetry prop threaded: `grep "onRetry" src/components/live-roleplay/LiveRoleplayPage.tsx src/components/live-roleplay/ConversationAnalysis.tsx` shows prop in both files
- handleRetryScenario preserves scenario: `grep "handleRetryScenario" src/components/live-roleplay/LiveRoleplayPage.tsx` shows function that does NOT call setScenario(null)
</verification>

<success_criteria>
- LiveRoleplayPage.handleRetryScenario sets turns=[] and phase='conversation' without clearing scenario
- ConversationAnalysis receives and uses onRetry prop
- 3-button layout: Tentar Novamente (primary), Novo Cenario (secondary), Ver Historico (ghost)
- "Nova Conversa" label replaced with "Novo Cenario"
- All existing tests pass
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/17-retry-exercise/17-02-SUMMARY.md`
</output>
