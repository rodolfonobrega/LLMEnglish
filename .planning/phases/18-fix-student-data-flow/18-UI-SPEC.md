---
phase: 18
slug: fix-student-data-flow
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-11
---

# Phase 18 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Phase Classification

**UI surface changes: NONE.**

Phase 18 is a pure TypeScript bug-fix phase. The two changes are:

1. `src/components/discovery/ExerciseMode.tsx` line 184 — rename variable `tempCardId` → `exerciseSessionId` and change the string prefix from `temp_` to `exercise_`. This is a call-site fix with no visual output.
2. `src/services/errorAnalysis.ts` — add `categoryToCardThemes` module-level constant and rewrite `getCardsForWeakArea` body. This is a pure service-layer change with no rendered output.

No new components, no new routes, no new UI states, no copy changes. All design tokens below are inherited from the existing system and declared here for completeness so the checker and executor have a complete contract.

**Source:** RESEARCH.md — "No new packages required. Phase touches existing project code only."

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | radix-ui + base-ui (existing, unchanged) |
| Icon library | lucide-react 0.563 (existing, no new icons added) |
| Font | Inter, system-ui, -apple-system, sans-serif (inherited from src/index.css) |

**Source:** RESEARCH.md Standard Stack + src/index.css body font declaration.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding |
| sm | 8px | Compact element spacing |
| md | 16px | Default element spacing |
| lg | 24px | Section padding |
| xl | 32px | Layout gaps |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing |

Exceptions: none

**Source:** Inherited from existing project design tokens (src/index.css). No spacing changes introduced in this phase.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px | 400 | 1.5 |
| Label | 14px | 500 | 1.5 |
| Heading | 20px | 600 | 1.2 |
| Display | 28px | 700 | 1.2 |
| Button | 16px | 600 | 1.2 |

**Source:** Inherited from existing project typography conventions. No typography changes introduced in this phase.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | hsl(var(--background)) | Page backgrounds, surfaces |
| Secondary (30%) | hsl(var(--card)) | Card backgrounds, secondary surfaces |
| Accent (10%) | hsl(var(--primary)) | Primary action buttons, active navigation states |
| Destructive | hsl(var(--danger)) | Not used in this phase |

Accent reserved for: Primary action buttons (existing `variant="primary"` Button component usage in ExerciseMode — unchanged).

**Source:** Inherited from existing CSS custom properties in src/index.css. No color changes introduced in this phase.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | Not applicable — no new CTAs in this phase |
| Empty state heading | Not applicable — no new empty states in this phase |
| Empty state body | Not applicable — no new empty states in this phase |
| Error state | Not applicable — no new error states in this phase |
| Destructive confirmation | Not applicable — no destructive actions in this phase |

**Note:** The `getCardsForWeakArea` function change affects what cards are returned for weak-area review, but the caller UI (weak area card display) is already implemented and uses existing copy. The data fix produces more accurate results — no copy changes required.

**Language:** All user-facing copy in the app is in Portuguese (Brazilian). This phase adds no user-facing copy.

---

## Component Inventory

### Existing Components Modified (logic change only — no visual change)

| Component | Location | Change |
|-----------|----------|--------|
| ExerciseMode | src/components/discovery/ExerciseMode.tsx | Call-site variable rename: `tempCardId` → `exerciseSessionId`, prefix `temp_` → `exercise_`. One-line logic change. No rendered output changes. |

### Existing Services Modified (no UI surface)

| Service | Location | Change |
|---------|----------|--------|
| errorAnalysis | src/services/errorAnalysis.ts | Add `categoryToCardThemes` constant; rewrite `getCardsForWeakArea` body; fix `guessCategory` false positive for preposition/article. No rendered output changes. |

### New Components

None.

### New Dependencies

None.

---

## Interaction Contract

No interaction changes in this phase.

The internal fix to `getCardsForWeakArea` means weak-area card surfaces (wherever `getCardsForWeakArea` is called) will return semantically filtered results. The interaction — clicking on weak-area recommendations — is unchanged. The fix is transparent to the user.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable |
| third-party | none | not applicable |

No new dependencies or registries introduced in this phase.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
