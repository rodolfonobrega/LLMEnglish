---
phase: 20-review-algorithm-fix
plan: 02
type: tdd
wave: 2
depends_on:
  - 20-01
files_modified:
  - src/services/supabase/storage.ts
  - src/services/supabase/storage.test.ts
autonomous: true
requirements:
  - REVI-02
  - REVI-04

must_haves:
  truths:
    - "Cards with NULL next_review_at appear in getCardsDueForReview() results"
    - "Cards with next_review_at in the past appear in getCardsDueForReview() results"
    - "Two reviews on the same day with the same score are both inserted into card_reviews"
    - "updateCard only inserts the LAST review in the array (the newly added one), not all historical reviews"
  artifacts:
    - path: "src/services/supabase/storage.ts"
      provides: "Orphan-inclusive query + dedup-free review insert"
      exports: ["getCardsDueForReview", "updateCard"]
    - path: "src/services/supabase/storage.test.ts"
      provides: "Tests for NULL query and same-day same-score review preservation"
  key_links:
    - from: "src/services/supabase/storage.ts"
      to: "supabase client"
      via: ".or() filter for NULL + .lte"
      pattern: "\\.or\\(.*next_review_at\\.is\\.null"
    - from: "src/components/review/ReviewPage.tsx"
      to: "src/services/storage.ts"
      via: "updateCard(updatedCard) after reviews.push"
      pattern: "updateCard"
---

<objective>
Fix the review queue query to include orphaned cards and remove the broken dedup logic that silently drops same-day same-score reviews.

Purpose: Orphaned cards (nextReviewAt=NULL) are invisible to the review system, and the date:score composite key dedup silently drops legitimate reviews. Both are data integrity bugs that cause users to lose work.

Output: Modified `storage.ts` with NULL-aware query and insert-last-only review persistence, plus extended test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-review-algorithm-fix/20-CONTEXT.md
@.planning/phases/20-review-algorithm-fix/20-RESEARCH.md
@.planning/phases/20-review-algorithm-fix/20-01-SUMMARY.md
@src/services/supabase/storage.ts
@src/services/supabase/storage.test.ts
</context>

<interfaces>
<!-- Key types the executor needs. From src/types/card.ts -->
```typescript
export interface ReviewEntry {
  date: string;
  score: number;
  userTranscription: string;
}

export interface Card {
  id: string;
  // ... other fields ...
  nextReviewAt?: string;
  reviews: ReviewEntry[];
  // ...
}
```

<!-- From src/types/supabase.ts -- CardReview shape used in storage.ts -->
```typescript
// CardReview has fields: id, card_id, user_id, date, score, user_transcription
// Used in storage.ts updateCard() dedup section
```

<!-- ReviewPage.tsx flow (confirmed by grep) -->
<!-- Line 89-96: updatedCard.reviews.push({date: new Date().toISOString(), score, userTranscription}); -->
<!-- Line 96: await updateCard(updatedCard); -->
<!-- This confirms: ONE review pushed, then updateCard called with full card including all historical reviews -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Orphan card query + dedup removal + storage tests</name>
  <files>src/services/supabase/storage.ts, src/services/supabase/storage.test.ts</files>
  <read_first>
    - src/services/supabase/storage.ts (file being modified -- especially getCardsDueForReview at line 248-270 and updateCard reviews section at lines 194-228)
    - src/services/supabase/storage.test.ts (existing test file to extend)
    - src/types/card.ts (Card, ReviewEntry interfaces)
    - src/types/supabase.ts (CardReview interface)
  </read_first>
  <behavior>
    - Test (mock): getCardsDueForReview() calls supabase query with .or() filter containing "next_review_at.is.null" -- verifies orphaned cards are included
    - Test (mock): updateCard() with a card containing 3 reviews only inserts the LAST review into card_reviews, not all 3
    - Test (mock): updateCard() with two reviews having same date and same score (same-day same-score scenario) -- both calls insert successfully (no dedup blocking)
  </behavior>
  <action>
    RED phase -- extend src/services/supabase/storage.test.ts:

    Add new describe blocks for the two bugs being fixed. These tests mock the supabase client. Follow the existing mock patterns in the file.

    1. Describe 'getCardsDueForReview - orphan cards':
       - Mock supabase chain to return cards where some have next_review_at: null
       - Verify the query uses .or() filter (not just .lte) -- check the mock was called with a string containing 'next_review_at.is.null'
       - Verify returned cards include the orphaned ones

    2. Describe 'updateCard - review persistence (no dedup)':
       - Mock supabase chain for card update + card_reviews select + card_reviews insert
       - Create a card with 3 reviews in .reviews array: two old + one new
       - Call updateCard(card)
       - Verify card_reviews.insert was called with ONLY the LAST review (index 2), not all 3
       - This confirms we no longer re-insert all historical reviews

    3. Describe 'updateCard - same-day same-score reviews preserved':
       - Mock supabase chain
       - Create a card with two reviews having identical date and score but different transcriptions
       - Call updateCard(card) for each review separately (simulating two review sessions)
       - Verify both inserts succeed (no dedup filter blocking the second one)

    Run tests -- the new tests MUST fail (RED) because:
    - getCardsDueForReview currently uses .lte() not .or()
    - updateCard currently uses date:score dedup that re-inserts all reviews

    GREEN phase -- modify src/services/supabase/storage.ts:

    FIX 1 -- getCardsDueForReview() orphan inclusion (D-03):

    Replace lines 252-261 (the query section):
                       const { data: cards, error } = await supabase
                           .from('cards')
                           .select(`
                               *,
                               card_reviews(*),
                               card_evaluations(*)
                           `)
                           .eq('user_id', userId)
                           .lte('next_review_at', now)
                           .order('next_review_at', { ascending: true })

    With:
                       const { data: cards, error } = await supabase
                           .from('cards')
                           .select(`
                               *,
                               card_reviews(*),
                               card_evaluations(*)
                           `)
                           .eq('user_id', userId)
                           .or(`next_review_at.is.null,next_review_at.lte.${now}`)
                           .order('next_review_at', { ascending: true })

    This uses Supabase's .or() filter to combine "next_review_at IS NULL" with "next_review_at <= now" in a single query. Cards with NULL next_review_at (orphaned cards) will now be included in the review queue.

    FIX 2 -- updateCard() dedup removal (D-05):

    Replace lines 194-228 (the entire reviews section):
                       // Persist reviews (dedup against existing)
                       if (updated.reviews && updated.reviews.length > 0) {
                           const { data: existingReviews } = await supabase
                               .from('card_reviews')
                               .select('date, score')
                               .eq('card_id', updated.id)

                           const existingKeys = new Set(
                               (existingReviews || []).map((r: { date: string; score: number }) =>
                                   `${r.date}:${r.score}`
                               )
                           )

                           const newReviews = updated.reviews.filter(r =>
                               !existingKeys.has(`${r.date}:${r.score}`)
                           )

                           if (newReviews.length > 0) {
                               const { error: reviewsError } = await supabase
                                   .from('card_reviews')
                                   .insert(
                                       newReviews.map(r => ({
                                           card_id: updated.id,
                                           user_id: userId,
                                           date: r.date,
                                           score: r.score,
                                           user_transcription: r.userTranscription,
                                       }))
                                   )

                               if (reviewsError) {
                                   console.error('Failed to persist reviews:', reviewsError.message)
                               }
                           }
                       }

    With:
                       // Persist only the most recently added review (last in array).
                       // ReviewPage pushes one review then calls updateCard with the full card.
                       // Inserting only the last avoids re-inserting all historical reviews.
                       // The DB serial id primary key guarantees row uniqueness.
                       if (updated.reviews && updated.reviews.length > 0) {
                           const latestReview = updated.reviews[updated.reviews.length - 1];

                           const { error: reviewsError } = await supabase
                               .from('card_reviews')
                               .insert({
                                   card_id: updated.id,
                                   user_id: userId,
                                   date: latestReview.date,
                                   score: latestReview.score,
                                   user_transcription: latestReview.userTranscription,
                               });

                           if (reviewsError) {
                               console.error('Failed to persist review:', reviewsError.message);
                           }
                       }

    Key differences from old code:
    - No SELECT query for existing reviews (eliminates the dedup read)
    - Only inserts the LAST review in the array, not all filtered reviews
    - Single insert instead of bulk insert
    - No date:score composite key dedup -- same-day same-score reviews are always preserved

    Run tests -- they MUST pass (GREEN).
  </action>
  <verify>
    <automated>npx vitest run src/services/supabase/storage.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - All storage tests pass (existing + new)
    - storage.ts getCardsDueForReview uses `.or()` filter (NOT `.lte()` alone)
    - storage.ts getCardsDueForReview `.or()` string contains `next_review_at.is.null`
    - storage.ts updateCard does NOT contain `existingKeys` or `date:score` dedup logic
    - storage.ts updateCard inserts only the LAST review from the array (single insert, not bulk)
    - No `card_reviews.select('date, score')` query remains in updateCard
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Supabase | Query parameters sent to Supabase client -- .or() filter string is constructed from trusted code (not user input) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-02 | I | getCardsDueForReview .or() filter | accept | Filter value is hardcoded code, not user input. Supabase client sanitizes query parameters. No injection risk. |
| T-20-03 | S | updateCard single-review insert | accept | userId from getCurrentUser() is server-validated. Review data is user's own content. |
</threat_model>

<verification>
- npx vitest run src/services/supabase/storage.test.ts exits 0
- grep -n '\.or(' src/services/supabase/storage.ts shows the new orphan-inclusive query in getCardsDueForReview
- grep -c 'existingKeys' src/services/supabase/storage.ts returns 0 (dedup removed)
- grep -c 'date.*score' src/services/supabase/storage.ts returns 0 (no composite key dedup)
- grep -n 'latestReview' src/services/supabase/storage.ts shows the new insert-last-only pattern in updateCard
</verification>

<success_criteria>
- getCardsDueForReview query includes cards with NULL next_review_at via .or() filter
- updateCard inserts only the last review in the array (not all historical reviews)
- No date:score composite key dedup remains in the codebase
- All existing storage tests continue to pass
- New tests for orphan inclusion and dedup removal pass
</success_criteria>

<output>
After completion, create `.planning/phases/20-review-algorithm-fix/20-02-SUMMARY.md`
</output>
