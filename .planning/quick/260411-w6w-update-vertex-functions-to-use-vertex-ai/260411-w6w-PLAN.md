---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/ai-proxy/index.ts
autonomous: true
requirements: [Q-01]

must_haves:
  truths:
    - "vertex* functions use API key auth (x-goog-api-key header) instead of OAuth Bearer token"
    - "vertex* functions call aiplatform.googleapis.com express endpoint instead of regional endpoint"
    - "source===vertex call sites use getApiKey(userId, 'genai') instead of getVertexAccessToken + getVertexConfig"
    - "get_vertex_live_token case still works with old OAuth path"
  artifacts:
    - path: "supabase/functions/ai-proxy/index.ts"
      provides: "Updated vertex functions and call sites"
  key_links:
    - from: "serve handler"
      to: "vertex* functions"
      via: "getApiKey(userId, 'genai')"
      pattern: "getApiKey\\(userId, 'genai'\\)"
---

<objective>
Update all vertex* functions in the edge function to use Vertex AI express mode with API key instead of requiring GCP project/OAuth.

Purpose: Simplify Vertex AI usage -- users only need their Gemini API key, no GCP project setup or service account configuration. Express mode uses the same API key as the genai source.

Output: Modified `supabase/functions/ai-proxy/index.ts` with simplified vertex functions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@supabase/functions/ai-proxy/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update vertex* function signatures and URL patterns to express mode</name>
  <files>supabase/functions/ai-proxy/index.ts</files>
  <action>
Update the five vertex* functions (`vertexChat`, `vertexChatWithImage`, `vertexTTS`, `vertexSTT`, `vertexImage`) to use Vertex AI express mode:

1. **Change signatures**: Replace `(accessToken: string, projectId: string, region: string, model: string, ...)` with `(apiKey: string, model: string, ...)` -- remove `accessToken`, `projectId`, `region` params.

2. **Change URL pattern**: Replace `https://${region}-aiplatform.googleapis.com/v1/projects/${projectId}/locations/${region}/publishers/google/models/${model}:ACTION` with `https://aiplatform.googleapis.com/v1/publishers/google/models/${model}:ACTION` (no region, no project).

3. **Change auth header**: Replace `'Authorization': Bearer ${accessToken}` with `'x-goog-api-key': apiKey`.

4. **Keep request body unchanged** -- the body format is identical between old and express mode.

Specific URL suffixes per function:
- `vertexChat`: `:generateContent`
- `vertexChatWithImage`: `:generateContent`
- `vertexTTS`: `:generateContent`
- `vertexSTT`: `:generateContent`
- `vertexImage` (Imagen path): `:predict`
- `vertexImage` (Gemini path): `:generateContent`

Do NOT modify `getVertexAccessToken()`, `getVertexConfig()`, `base64urlEncode()`, `uint8ToBase64url()`, `str2ab()` -- they are still used by the `get_vertex_live_token` case. Do NOT modify `sourceToDbColumn` -- it should still return `null` for 'vertex'.
  </action>
  <verify>
    <automated>grep -c "x-goog-api-key" supabase/functions/ai-proxy/index.ts</automated>
  </verify>
  <done>All five vertex* functions accept (apiKey, ...) and use express endpoint URL with x-goog-api-key header. No references to projectId/region remain in vertex* function bodies. getVertexAccessToken and getVertexConfig functions remain untouched for get_vertex_live_token case.</done>
</task>

<task type="auto">
  <name>Task 2: Update all source==='vertex' call sites in the serve handler</name>
  <files>supabase/functions/ai-proxy/index.ts</files>
  <action>
Update every call site in the `serve` handler where `source === 'vertex'` to use the simplified function signatures:

Replace all patterns of:
```typescript
const accessToken = await getVertexAccessToken()
const { projectId, region } = await getVertexConfig(userId)
result = await vertexX(accessToken, projectId, region, ...)
```

With:
```typescript
const apiKey = await getApiKey(userId, 'genai')
if (!apiKey) throw new Error('No Gemini API key configured for Vertex AI')
result = await vertexX(apiKey, ...)
```

There are 5 call sites to update:
1. **chat / imageMode vertex path** (vertexChatWithImage) -- around line 1238-1260
2. **chat / regular vertex path** (vertexChat) -- around line 1323-1326
3. **tts vertex path** (vertexTTS) -- around line 1358-1361
4. **stt vertex path** (vertexSTT) -- around line 1391-1394
5. **image vertex path** (vertexImage) -- around line 1425-1428

Do NOT touch the `get_vertex_live_token` case (around line 1452-1460) -- it still uses the old OAuth path for WebSocket connections.
  </action>
  <verify>
    <automated>grep -c "getVertexAccessToken" supabase/functions/ai-proxy/index.ts && grep -c "getVertexConfig" supabase/functions/ai-proxy/index.ts</automated>
  </verify>
  <done>All 5 vertex call sites use getApiKey(userId, 'genai') and call the simplified vertex*(apiKey, ...) functions. get_vertex_live_token case still uses getVertexAccessToken + getVertexConfig. No orphaned references to projectId/region in vertex call sites (only in get_vertex_live_token and the getVertexConfig/getVertexAccessToken function definitions).</done>
</task>

</tasks>

<verification>
1. `grep "x-goog-api-key" supabase/functions/ai-proxy/index.ts` -- should show 5 matches (one per vertex function)
2. `grep "getVertexAccessToken" supabase/functions/ai-proxy/index.ts` -- should only appear in function definition and get_vertex_live_token case
3. `grep "getVertexConfig" supabase/functions/ai-proxy/index.ts` -- should only appear in function definition and get_vertex_live_token case
4. `grep "aiplatform.googleapis.com/v1/publishers" supabase/functions/ai-proxy/index.ts` -- all vertex URLs use express format (no region/project)
5. No TypeScript errors: `npx tsc --noEmit` (or review for type correctness)
</verification>

<success_criteria>
- All 5 vertex* functions use express endpoint: `aiplatform.googleapis.com/v1/publishers/google/models/{model}:action`
- All 5 vertex* functions authenticate via `x-goog-api-key` header
- All 5 vertex call sites fetch key via `getApiKey(userId, 'genai')`
- get_vertex_live_token case still uses old OAuth path (unchanged)
- No dead code (getVertexAccessToken and getVertexConfig functions remain for live token use)
</success_criteria>

<output>
After completion, create `.planning/quick/260411-w6w-update-vertex-functions-to-use-vertex-ai/260411-w6w-SUMMARY.md`
</output>
