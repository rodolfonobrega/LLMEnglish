services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: llmenglish-dev
    working_dir: /workspace
    volumes:
      - .:/workspace
    stdin_open: true
    tty: true
    ports:
      - "5173:5173"
    command: ["bash", "-lc", "exec bash"]
